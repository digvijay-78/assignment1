
class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        """
        Do not return anything, modify nums1 in-place instead.
        """
        l=0
        r=0
        s=[]
        while r<(len(nums1) or len(nums2)):
            if nums1[l]<nums2[r] :
                s.append(numsl)
                s.append(nums2)
                l+=1
                r+=1 
            elif num1[l]>nums2[r]:
                s.append(nums2)
                s.append(nums1)
                l+=1
                r+=1
            elif nums1[l]==0:
                l+=1
            elif nums2[r]==0:
                r+=1